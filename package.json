{
  "private": true,
  "scripts": {
    "build": "spago build --purs-args '--censor-lib --strict'",
    "halogen-example": "spago --config halogen-example/spago.dhall bundle-app --main HalogenExample.Main --to halogen-example/dist/fileio.js",
    "pulp-build": "pulp build"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "pulp": "^16.0.0",
    "purescript-psa": "^0.5.1"
  }
}
