{
  "private": true,
  "scripts": {
    "build": "spago build",
    "example": "spago bundle --package halogen-example --outfile dist/fileio.js --bundle-type app --platform browser"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "purescript": "0.15.15",
    "purescript-psa": "^0.5.1",
    "spago": "^0.93.42"
  }
}
